# Themes

Demonstrates using built-in themes, creating custom themes with `background` and `surface` fields, and switching between themes.

## Source

{{Include:examples/themes.zig}}

## Running

```bash
zig build run-themes
```
